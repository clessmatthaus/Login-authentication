<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.clesspavy.login_form">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="CM App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginForm">
        <activity android:name=".Inscription" android:theme="@style/Theme.Noaction">
         </activity>
        <activity android:name=".BaseIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:theme="@style/Theme.Noaction">

        </activity>
    </application>

</manifest>